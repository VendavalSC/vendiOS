# Build artifacts
out/
work/
*.iso
*.pkg.tar.zst
*.pkg.tar.xz

# Editor / OS
*.swp
*.swo
.DS_Store
.vscode/
.idea/

# Logs
*.log

# pkg build trees
pkg/*/src/
pkg/*/pkg/
pkg/*/*.tar.gz

# Rust workspace build outputs (Cargo.lock IS committed — binary crate)
src/target/

# Rust binaries staged into the ISO by build.sh (rebuilt each run)
archiso/airootfs/usr/bin/vendiwm
archiso/airootfs/usr/bin/vendi-ctl
archiso/airootfs/usr/bin/vendi-demo

# QEMU test fixtures (disk image, OVMF NVRAM copy)
vm/

# vendi-buds is personal/this-machine-only (see build.sh) — this product
# photo isn't ours to redistribute, so it must never reach the public repo.
src/vendibar-pro/airpods.png
archiso/airootfs/etc/xdg/quickshell/vendibar-pro/airpods.png
# Claude Code gadget icon — local-only too (see build.sh / extras/).
archiso/airootfs/etc/xdg/quickshell/vendibar-pro/claude.svg

# Offline package repo — generated by build.sh --offline (downloaded
# packages + signatures + db), wiped on online/--clean builds. Never track it.
archiso/airootfs/opt/vendios/
