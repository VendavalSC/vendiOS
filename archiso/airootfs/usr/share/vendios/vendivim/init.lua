-- vendiVim — LazyVim base + vendiOS theme integration.
-- Deployed to ~/.config/nvim by `vendi dev`. The colorscheme follows
-- `vendi theme`: switching the desktop theme rewrites ~/.config/vendi/nvim.lua,
-- which the `vendi` colorscheme reads on next launch.
require("config.lazy")
